{% trans_default_domain 'admin.wb.products.settings' %}

<div class="d-flex">
    <div class="w-100">
        {{ form_row(form.category, { label: 'Категория', placeholder: 'Выберите Категорию из списка...', attr: { 'data-select' : 'select2' } }) }}
    </div>
</div>

<div class="d-flex justify-content-between align-items-center gap-3">

    <div class="w-50">
        {{ form_row(form.counter, { label : 'Количество стикеров' }) }}
    </div>

    <div class="w-50">
        <label class="form-label">&nbsp;</label>
        {{ form_row(form.offer, { label : 'Торговое предложение' }) }}
    </div>

    <div class="w-50">
        <label class="form-label">&nbsp;</label>
    {{ form_row(form.variation, { label : 'Множественный вариант' }) }}
    </div>

</div>

